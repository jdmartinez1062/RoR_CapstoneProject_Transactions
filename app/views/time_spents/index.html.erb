<%=page_heading(time_spents_title)%>
<div>
    <div class="d-flex flex-row align-items-center mb-3">
        <%if group_times?%>
        <%=image_tag(@group.icon.url, alt: 'Image', class: 'profile-image')%>
        <%end%>
        <div class="ml-3 d-flex">

            <h3 class="ml-1 group-name"><%=@group.name%></h3>
        </div>

    </div>


    <h4 class="total">Total hours:</h4>
    <p class="total"><%=@total%></p>
</div>


<%=will_paginate @time_spents%>
<div><%=render partial: 'shared/time_spent', collection: @time_spents%></div>
<%=will_paginate @time_spents%>